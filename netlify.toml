[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (e.g. /2024)
# so deep links and refreshes don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
